---
- name: Configure Puppet autosigning rules
  lineinfile:
    dest: "{{ puppet_vars.dir }}/autosign.conf"
    line: "{{ item }}"
  with_items:
    - "{{ puppet_vars.autosign_rules }}"
  tags:
    - puppet
    - autosign
